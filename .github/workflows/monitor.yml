name: monitor
on:
  workflow_dispatch:   # manual
  schedule:
    - cron: "0 2 * * *"  # daily 2 AM UTC

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Monitor task
        run: echo "Running daily monitoring at 2 AM UTC"
